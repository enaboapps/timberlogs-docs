name: Aggregate Mintlify Docs

on:
  push:
    branches: [main]
  repository_dispatch:
    types: [docs-update]
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  aggregate-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: mintlify/multirepo-action@v0.16
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target-branch: docs
          repos: |
            - owner: enaboapps
              repo: timberlogs-typescript-sdk
              subdirectory: docs
            - owner: enaboapps
              repo: timberlogs-python-sdk
              subdirectory: docs
            - owner: enaboapps
              repo: timberlogs-cli
              subdirectory: docs
